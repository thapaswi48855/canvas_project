import { LayersnamesPipe } from './layersnames.pipe';

describe('LayersnamesPipe', () => {
  it('create an instance', () => {
    const pipe = new LayersnamesPipe();
    expect(pipe).toBeTruthy();
  });
});
